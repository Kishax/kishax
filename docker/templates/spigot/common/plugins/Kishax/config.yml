# Configuration File
Menu:
  Server: true
  ImageMap: true

Portals:
  Move: true
  Wand: true

MySQL:
  Host: "${MYSQL_HOST}"
  Database: "${MYSQL_DATABASE}"
  Port: ${MYSQL_PORT}
  User: "${MYSQL_USER}"
  Password: "${MYSQL_PASSWORD}"

Socket:
  Server_Port:

AutoStop:
  Mode: false
  Interval: 3
